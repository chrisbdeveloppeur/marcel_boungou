{% extends 'base.html.twig' %}

{% block title %}{% trans %}Book index{% endtrans %}{% endblock %}

{% block body %}
    <h1 class="title has-text-white has-text-centered pt-3">{% trans %}Book index{% endtrans %}</h1>

    <div class="column is-flex is-justify-content-center is-flex-direction-row">
        <div class="px-1 py-1">
            {% include 'includes/index_entity_buttons/global_btn.html.twig' with {'action' : 'index','type':'success', 'entity' : null, 'entity_type' : 'bibliography', 'text': 'Back to Bibliography page'|trans } %}
        </div>
        <div class="px-1 py-1">
            {% include 'includes/index_entity_buttons/global_btn.html.twig' with {'action' : 'new','type':'success', 'entity' : null, 'entity_type' : 'book', 'text': 'Add'|trans } %}
        </div>
    </div>

<div class="table-container">
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>{% trans %}Title{% endtrans %}</th>
                <th>{% trans %}Summary{% endtrans %}</th>
                <th>{% trans %}Shop link{% endtrans %}</th>
                <th>{% trans %}Tags{% endtrans %}</th>
                <th>{% trans %}Image{% endtrans %}</th>
                <th>{% trans %}Actions{% endtrans %}</th>
            </tr>
        </thead>
        <tbody>
        {% for book in books %}
            <tr>
                <td>{{ book.id }}</td>
                <td>{{ book.title }}</td>
                <td>{{ book.description|raw }}</td>
                <td><a href="{{ book.redirectLink }}">{{ book.redirectLink }}</a></td>
                <td>{{ book.tags }}</td>
                <td>{{ book.image }}</td>
                <td>
                    <div class="is-flex">
                        {% include 'includes/index_entity_buttons/global_btn.html.twig' with {'action' : 'show', 'type':'success', 'entity' : book, 'entity_type' : 'book', 'text': 'Show'|trans } %}
                        {% include 'includes/index_entity_buttons/global_btn.html.twig' with {'action' : 'edit', 'type':'success', 'entity' : book, 'entity_type' : 'book', 'text': 'Show'|trans } %}
                        {% include 'includes/index_entity_buttons/global_btn.html.twig' with {'action' : 'delete', 'type':'danger', 'entity' : book, 'entity_type' : 'book', 'text': 'Delete book'|trans, 'confirm_msg': 'Are you sure you want to delete this item?'|trans } %}
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">{% trans %}no records found{% endtrans %}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>

    <div class="column is-flex is-justify-content-center is-flex-direction-row">
        <div class="px-1 py-1">
            {% include 'includes/index_entity_buttons/global_btn.html.twig' with {'action' : 'index','type':'success', 'entity' : null, 'entity_type' : 'bibliography', 'text': 'Back to Bibliography page'|trans } %}
        </div>
        <div class="px-1 py-1">
            {% include 'includes/index_entity_buttons/global_btn.html.twig' with {'action' : 'new', 'type':'success', 'entity' : null, 'entity_type' : 'book', 'text': 'Add'|trans } %}
        </div>
    </div>

{% endblock %}
