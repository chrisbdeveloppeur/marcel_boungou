{% extends 'base.html.twig' %}

{% block title %}{% trans %}Home{% endtrans %}{% endblock %}

{% block body %}

    <div class="is-relative has-background-black">
        <div class="ancrage-home-sections" id="welcome"></div>
        {{ include('home/sections/welcome.html.twig') }}
    </div>

    <div class="is-relative has-background-black">
        {{ include('home/sections/news.html.twig') }}
    </div>

    <div class="is-relative">
        {{ include('home/sections/events.html.twig') }}
    </div>

    <div class="is-relative">
        {{ include('home/sections/mediatheque.html.twig') }}
    </div>

    <div class="is-relative">
        {{ include('home/sections/mb_ministry.html.twig') }}
    </div>











{% endblock %}
