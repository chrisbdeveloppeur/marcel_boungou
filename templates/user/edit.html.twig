{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}

    <div class="column is-two-fifths-desktop is-two-thirds-tablet has-background-light">
        <h1 class="subtitle is-3">{% trans %}Edit User{% endtrans %}</h1>

        {{ include('themes/_form.html.twig') }}

        <div class="is-flex is-justify-content-space-between">
            <a class="button is-small is-success" href="{{ path('user_index') }}">{% trans %}back to list{% endtrans %}</a>

            {{ include('user/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
